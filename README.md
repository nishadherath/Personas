# Personas

Agentic persona library for different tasks, for different classes of models.

The model class capabilities are using Anthropic models classes Fable, Opus, Sonnet and Haiku as the standard.

For token efficiency, files are structured along model class and programming language.
